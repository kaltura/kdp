# This is the build properties file

# Project properties
project.bin=${basedir}/bin
project.src=${basedir}/src
project.lib=${basedir}/flash
project.outputfile=${project.bin}/overlayPlugin.swf
project.asfile=${basedir}/src/overlayPlugin.as
project.configfile=${solution.configfile}
# This is the build properties file

# Project properties
project.bin=${basedir}/bin
project.src=${basedir}/src
project.lib=${basedir}/libs
project.outputfile=${project.bin}/captureThumbnailPlugin.swf
project.asfile=${basedir}/src/captureThumbnailPlugin.as
project.configfile=${solution.configfile}
# This is the build properties file

# Project properties
project.bin=${basedir}/bin
project.src=${basedir}/
project.outputfile=${project.bin}/VASTNew.swc
project.configfile=${basedir}/vast-build-config.xml

